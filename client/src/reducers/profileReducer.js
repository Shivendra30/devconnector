const initialState = {
	profile: null,
	profiles: null,
	loading: false
};
//loading will be used to show a spinner

export default (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
